import 'package:flutter/material.dart';

class Venda extends StatelessWidget {
  static String route = "/venda";
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}